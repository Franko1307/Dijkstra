#pragma once
template <typename _InputIter> class lista_arco;
template <typename _InputIter> struct nodo_aux;
template <typename _InputIter> struct nodo;
enum DONDE { VACIO,INICIO, MITAD, FINAL, MIFA };
